import Toybox.Lang;

// Stub -- the diaper-instant-record task replaces this with the real
// enqueue-and-show-SuccessView flow. HomeDelegate already persists
// Store.lastAction on every activation, so this has nothing to do yet.
module RecordController {

    function recordDiaper(action as String) as Void {
    }

}
